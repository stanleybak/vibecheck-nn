# Virtual environment
.venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
dist/
build/

# Local paths config
tests/paths.yaml

# Gurobi licensing is local-only -- never commit. The real node-locked license
# lives solely in the VNNCOMP web form's post-install field, so post_install_tool.sh
# (which holds it) is kept out of git entirely.
gurobi.lic
vnncomp_scripts/post_install_tool.sh

# Pytest
.pytest_cache/
htmlcov/
.coverage

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# development files
run_*.py
# ...but the committed VNNCOMP batch runner is not a scratch script
!vnncomp_scripts/run_benchmarks.py
scratch/

# Claude Code local config (agents, skills, periodic-campaign loop) — local-only,
# not part of the published tool.
.claude/
